# DEPRECATED - Workflow Separation Complete
# 
# This workflow has been replaced by specialized workflows:
# - pr-validation.yml: Branch naming and PR title validation
# - milestone-validation.yml: Comprehensive milestone feature validation  
# - post-merge-automation.yml: Auto-tagging and release creation
# - core-build-test.yml: Basic build and test for all branches
# - hotfix-validation.yml: Fast security and critical path testing
# - docs-validation.yml: Documentation link checking and format validation
#
# Each workflow is optimized for specific branch types and purposes
# according to the Branch-Specific CI Strategy defined in
# BRANCH_SPECIFIC_CI_STRATEGY.md
#
# This file is kept for historical reference and will be removed
# once all workflows are validated.

name: Version Management (DEPRECATED)

on:
  # Disabled - functionality moved to specialized workflows
  workflow_dispatch:
    inputs:
      legacy_mode:
        description: 'Enable legacy mode (for emergencies only)'
        required: false
        default: 'false'

permissions:
  contents: read

jobs:
  deprecated-notice:
    runs-on: ubuntu-latest
    if: github.event.inputs.legacy_mode != 'true'
    
    steps:
      - name: Workflow Deprecation Notice
        run: |
          echo "⚠️ DEPRECATED WORKFLOW"
          echo "==================="
          echo ""
          echo "This workflow has been replaced by specialized workflows:"
          echo ""
          echo "📋 For Pull Request Validation:"
          echo "   • pr-validation.yml - Branch naming and PR title validation"
          echo "   • core-build-test.yml - Basic build and test for all branches"
          echo ""
          echo "🎯 For Branch-Specific Validation:"
          echo "   • milestone-validation.yml - Comprehensive milestone feature validation"
          echo "   • hotfix-validation.yml - Fast security and critical path testing"
          echo "   • docs-validation.yml - Documentation validation"
          echo ""
          echo "� For Post-Merge Automation:"
          echo "   • post-merge-automation.yml - Auto-tagging and release creation"
          echo ""
          echo "📚 See BRANCH_SPECIFIC_CI_STRATEGY.md for the complete strategy"
          echo ""
          echo "✅ Workflow separation complete - better performance and maintainability"
